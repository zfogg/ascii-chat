# ascii-chat pkg-config file (SHARED library)
# For use with pkg-config to get compile and link flags
#
# Usage:
#   pkg-config --cflags --libs ascii-chat
#
# For static linking, use ascii-chat-static instead:
#   pkg-config --cflags --libs ascii-chat-static

prefix=@CMAKE_INSTALL_PREFIX@
exec_prefix=${prefix}
libdir=${exec_prefix}/lib
includedir=${prefix}/include

Name: ascii-chat
Description: Real-time terminal-based video chat with ASCII art conversion (shared library)
Version: @PROJECT_VERSION@
URL: @PROJECT_HOMEPAGE_URL@

# Shared library needs runtime dependencies
Requires.private: @MIMALLOC_REQUIREMENT@libsodium >= 1.0.18, libzstd, portaudio-2.0

# Main shared library to link
Libs: -L${libdir} -lasciichat

# System libraries
Libs.private: -lm -lpthread

# Include directories
Cflags: -I${includedir} -I${includedir}/ascii-chat
