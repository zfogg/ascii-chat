# ascii-chat pkg-config file (FAT STATIC library)
# For use with pkg-config to get compile and link flags
#
# Usage:
#   pkg-config --cflags --libs ascii-chat-static
#
# This is the fat static library with all dependencies baked in.
# Only system libraries need to be linked.
#
# For shared/dynamic linking, use ascii-chat instead:
#   pkg-config --cflags --libs ascii-chat

prefix=@CMAKE_INSTALL_PREFIX@
exec_prefix=${prefix}
libdir=${exec_prefix}/lib
includedir=${prefix}/include

Name: ascii-chat-static
Description: Real-time terminal-based video chat with ASCII art conversion (fat static library)
Version: @PROJECT_VERSION@
URL: @PROJECT_HOMEPAGE_URL@

# No Requires - all dependencies are baked into the fat static archive

# Fat static library (includes libsodium, zstd, portaudio, opus, mimalloc, webrtc)
Libs: -L${libdir} -lasciichat

# Platform system libraries only
# macOS: Add frameworks via LDFLAGS or in your build system
# Linux: pthread, math, dl, stdc++
Libs.private: -lm -lpthread -ldl -lstdc++

# Include directories
Cflags: -I${includedir} -I${includedir}/ascii-chat
