# ascii-chat pkg-config file
# For use with pkg-config to get compile and link flags
#
# Usage:
#   pkg-config --cflags --libs ascii-chat

prefix=@CMAKE_INSTALL_PREFIX@
exec_prefix=${prefix}
libdir=${exec_prefix}/lib
includedir=${prefix}/include

Name: ascii-chat
Description: Real-time terminal-based video chat with ASCII art conversion
Version: @PROJECT_VERSION@
URL: @PROJECT_HOMEPAGE_URL@

# Dependencies with pkg-config files
Requires.private: libsodium >= 1.0.18, libzstd, portaudio-2.0

# Main library to link
Libs: -L${libdir} -lasciichat

# Additional libraries needed for static linking
Libs.private: -lm -lpthread

# Include directories
Cflags: -I${includedir}
