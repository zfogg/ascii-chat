:5173 {
  root * /app/www/dist
  file_server

  # Proxy /api requests to the backend
  reverse_proxy /api/* localhost:30001

  # SPA routing: fall back to index.html for client-side routing
  try_files {path} /index.html
}
