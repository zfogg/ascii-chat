:80 {
	root * /app/discovery/dist

	# Security headers from vercel.json
	header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
	header X-Content-Type-Options "nosniff"
	header X-Frame-Options "SAMEORIGIN"
	header X-XSS-Protection "1; mode=block"
	header Referrer-Policy "strict-origin-when-cross-origin"
	header Permissions-Policy "camera=(), microphone=(), geolocation=()"

	# Proxy font requests to ascii-chat.com (fonts served from one place)
	handle /fonts/* {
		reverse_proxy https://ascii-chat.com {
			header_up Host ascii-chat.com
		}
	}

	# Cache static assets with content hashes for 1 year (immutable)
	handle /assets/* {
		header Cache-Control "public, max-age=31536000, immutable"
		file_server
	}

	# Rewrites: / and /404 rewrite to index.html
	@sparoutes {
		path / /404
	}
	handle @sparoutes {
		rewrite * /index.html
		header Cache-Control "public, max-age=3600"
		file_server
	}

	# Default file server for remaining files (1 hour cache)
	handle {
		header Cache-Control "public, max-age=3600"
		file_server
	}

	# Handle 404 errors for non-existent routes
	handle_errors 404 {
		rewrite * /404.html
		file_server
	}
}
