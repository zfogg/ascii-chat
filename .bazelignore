# Directories to ignore during Bazel builds
# These contain CMake build artifacts and cached dependencies

# CMake build directories
build
build_release
build_debug
build_panic
build_check
build_test

# CMake cached deps that contain problematic symlinks (alsa-lib, etc.)
# Note: .deps-cache/defer-tool is NOT here - we need it for Bazel defer transformations
.deps-cache/Release
.deps-cache/Debug
.deps-cache/Dev

# IDE and editor directories
.idea
.vscode

# Git directory
.git
