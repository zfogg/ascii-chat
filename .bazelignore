# Directories to ignore during Bazel builds
# These contain CMake build artifacts and cached dependencies

# CMake build directories
build
build_release
build_debug
build_panic

# Note: .deps-cache is NOT ignored - it contains the pre-built defer tool
# that Bazel needs for defer() transformations

# IDE and editor directories
.idea
.vscode

# Git directory
.git
