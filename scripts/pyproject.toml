[project]
name = "ascii-chat-wordgen"
version = "0.1.0"
description = "Wordlist generator for ACDS session strings"
requires-python = ">=3.8"
dependencies = [
    "nltk>=3.8",
]

[project.scripts]
generate-wordlists = "generate_wordlists:main"
